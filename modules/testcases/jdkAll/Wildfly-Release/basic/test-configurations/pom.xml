<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.jboss</groupId>
        <artifactId>wildfly-release-additional-testsuite-basic</artifactId>
        <version>1.0.1.Final-SNAPSHOT</version>
    </parent>

    <artifactId>wildfly-release-additional-testsuite-basic-configuration</artifactId>
    <name>wildfly release additional testsuite: basic : configuration</name>
            
    <properties>
	<standalone.conf>${basedir}/../src/test/config/standaloneWildfly/standalone.xml</standalone.conf>
        <jboss.modules.dir>${basedir}/../../../../..</jboss.modules.dir>
        <surefire.memory.args>-Xmx512m -XX:MaxPermSize=256m</surefire.memory.args>
        <surefire.system.args>${surefire.memory.args} -Djboss.dist=${jboss.modules.dir}/wildfly/build/target/jbossas -DjbossHome=${jboss.modules.dir}/wildfly/build/target/jbossas</surefire.system.args>
        <server>wildfly</server>
    </properties>
        
    <build>

        <!--
	    Surefire test executions
	 -->
	 <plugins>  
		            
	    <plugin>
		<groupId>org.apache.maven.plugins</groupId>
		<artifactId>maven-surefire-plugin</artifactId>

                 <configuration>
                    <!-- Prevent test and server output appearing in console. -->
                    <redirectTestOutputToFile>${testLogToFile}</redirectTestOutputToFile>

                    <systemPropertyVariables>
                        <jboss.options>${surefire.system.args}</jboss.options>
                        <jboss.home>${jboss.modules.dir}/wildfly/build/target/jbossas</jboss.home>
                        <module.path>${jboss.modules.dir}/wildfly/build/target/jbossas/modules</module.path>
                    </systemPropertyVariables>

                    <!-- Arquillian's config files. -->
                    <additionalClasspathElements combine.children="append">
                        <additionalClasspathElement>${jboss.modules.dir}/src/config/arqWildfly</additionalClasspathElement>
                    </additionalClasspathElements>

                </configuration>

		<executions>
	            <execution>
		        <id>default-test</id>
		        <phase>test</phase>
		        <goals>
		            <goal>test</goal>
		        </goals>
		        <configuration>         
		            <!-- Tests to execute. -->
		            <includes>
		                <include>org/jboss/additional/testsuite/jdkall/basic/**/BasicTest.java</include>
		            </includes> 
		        </configuration>
		    </execution>
		</executions>
	    </plugin>
		            
            <!-- Build the server configuration -->
	    <plugin>
	        <groupId>org.apache.maven.plugins</groupId>
	        <artifactId>maven-antrun-plugin</artifactId>
	        <executions combine.children="append">
	            <execution>
	                <id>build-basic-config</id>
	                <phase>process-test-resources</phase>
	                <goals>
	                    <goal>run</goal>
	                </goals>
	                <configuration>
	                    <target>
	                        <ant antfile="${jboss.modules.dir}/src/test/scripts/basic-build.xml">
	                            <property name="workspace" value="/${jboss.modules.dir}" />
                                    <property name="server" value="${server}" />
	                            <property name="standaloneConfiguration" value="${standalone.conf}" />
	                            <target name="build-basic"/>
	                        </ant>
	                    </target>
	                </configuration>
	            </execution>
	        </executions>
	    </plugin>

        </plugins>
    </build>

</project>
