<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <jboss-ejb-client xmlns="urn:jboss:wildfly-client-ejb:3.0">
        <connections>
            <connection uri="remote+http://127.0.0.1:8080"/>
        </connections>
    </jboss-ejb-client>
    <!--
    workaround for connection close issue due to write-timeout.
    Set heartbeat-interval to a value shorter than write-timeout in http-listener on server-side.
    -->
    <!--
    <endpoint xmlns="urn:jboss-remoting:5.1" heartbeat-interval="3000">
        <connections/>
    </endpoint>
    -->
</configuration>